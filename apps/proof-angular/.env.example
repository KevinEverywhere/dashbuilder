# Map fallbacks when Settings BYOK vault is empty (mirror proof-react).
VITE_GOOGLE_MAPS_API_KEY=
VITE_MAPTILER_API_KEY=

# --- Backend parity (Stack tab live demo) ---
# Dev server proxies /parity-api → NestJS parity container (:53101).
# VITE_PARITY_API_URL=http://127.0.0.1:53101/api
