# Angular sports scoreboard

[DAS-169](https://planetkevin.atlassian.net/browse/DAS-169) composite demo. Scores come from ESPN’s public site API through the Vite dev proxy, with a sample board if the feed is empty.

## Run

```bash
npm run demo:sports
```

Opens [http://localhost:4324](http://localhost:4324).
