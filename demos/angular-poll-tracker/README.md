# Angular poll tracker

[DAS-169](https://planetkevin.atlassian.net/browse/DAS-169) composite demo. Races are a bundled snapshot so the widget runs without a polling-API key.

## Run

```bash
npm run demo:poll
```

Opens [http://localhost:4325](http://localhost:4325).
